name: sidecar_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 0.1.6
publish_to: none

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  sidecar: ^0.1.6
