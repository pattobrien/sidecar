class SidecarRuleConfiguration {
  const SidecarRuleConfiguration();
}

class SidecarPackageConfiguration {
  const SidecarPackageConfiguration();
}

const sidecarRuleConfiguration = SidecarRuleConfiguration();

const sidecarPackageConfiguration = SidecarPackageConfiguration();
